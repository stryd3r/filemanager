package com.filemanager.service;

import org.springframework.stereotype.Service;

@Service
public class QuestionServiceImpl {

	/*
	 * @Autowired private QuestionRepository questionRepository;
	 * 
	 * public List<QuestionModel> getQuestions(boolean onlyUsed) { return questionRepository.getQuestions(onlyUsed); }
	 * 
	 * @Override public boolean insertQuestion(QuestionModel question) {
	 * 
	 * return questionRepository.insertQuestion(question); }
	 * 
	 * @Override public boolean updateQuestion(QuestionModel question) {
	 * 
	 * return questionRepository.updateQuestion(question); }
	 * 
	 * @Override public boolean removeQuestion(int questionId) {
	 * 
	 * return questionRepository.removeQuestion(questionId); }
	 */

}
